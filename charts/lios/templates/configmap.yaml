apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lios.fullname" . }}
  labels:
    {{- include "lios.labels" . | nindent 4 }}
data:
  config.override.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}

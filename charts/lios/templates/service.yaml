apiVersion: v1
kind: Service
metadata:
  name: {{ include "lios.fullname" . }}
  labels:
    {{- include "lios.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lios.selectorLabels" . | nindent 4 }}
